function parse(input) {
    var text = input[0];
    var parsedtext = '';
    var textArr =text.split(' ');
    formatText (textArr);


    function formatText(textArr) {
       textArr.forEach(function (word) {

         //TODO finish later

       });
    }

}
parse([ 'We are <orgcase>liViNg</orgcase> in a <upcase>yellow submarine</upcase>. We <orgcase>doN\'t</orgcase> have <lowcase>anything</lowcase> else.' ]);


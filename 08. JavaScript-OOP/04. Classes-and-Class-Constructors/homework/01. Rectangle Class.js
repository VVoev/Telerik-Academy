function solve() {
    class Rectangle {
        constructor(width, height, color) {
            this.width = width;
            this.height = height
            this.color = color;
        }

        calcArea() {
            return// this.width*this.height;
        }
    }
}


arr = [5, 8, 'green'];

solve(arr);
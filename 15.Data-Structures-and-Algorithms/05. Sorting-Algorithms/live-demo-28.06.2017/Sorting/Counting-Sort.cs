namespace Sorting
{
   static class CountingSort
    {
        public static void Counting_Sort(this int[] array, int minNumber, int maxNumber)
        {
            var counts = new int[maxNumber + 1 - minNumber];

            foreach (var x in counts)
            {

            }
        }
    }
}
